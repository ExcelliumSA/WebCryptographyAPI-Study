{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "npx http-server -p 8443 -S -C cert.pem -K key.pem -c-1",
            "name": "StartWebServerWithHTTPS",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}